import type { ComponentMeta } from '../../../component-meta';

export const meta: ComponentMeta = {
  label: 'Card',
  description: 'Bordered content surfaces with default and raised elevation, optionally a whole-card link.',
  order: 140,
};
