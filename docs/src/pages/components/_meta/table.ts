import type { ComponentMeta } from '../../../component-meta';

export const meta: ComponentMeta = {
  label: 'Table',
  description: 'Accessible data tables with a required caption, column headers, and numeric alignment.',
  order: 130,
};
