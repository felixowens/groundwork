import type { ComponentMeta } from '../../../component-meta';

export const meta: ComponentMeta = {
  label: 'Tag',
  description: 'Compact status indicators with required text labels and closed variants.',
  order: 120,
};
